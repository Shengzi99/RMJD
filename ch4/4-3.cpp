#include<cstdio>
#include<cstring>

#define nmax 25

int n, k, m;
char a[nmax];
int go(int c)
int main(){
    while(scanf("%d%d%d", &n, &k, &m)==3 && n==0){
        memset(a, 1, sizeof(a));

    }

    return 0;
}